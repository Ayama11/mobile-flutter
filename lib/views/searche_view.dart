import 'package:flutter/material.dart';

class SearcheView extends StatelessWidget {
  const SearcheView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
    );
  }
}
